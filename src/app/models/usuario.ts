//para crear esta clase se ejecuta el siguiente comando ng g cl models/usuaros
export class Usuario {

    //declararemos los atrbutos de esta clase
    id: number | undefined;
    usuario: string | undefined;
    password: string | undefined;
    //Una ves declarado vamos a component-register-user.component.ts
}
